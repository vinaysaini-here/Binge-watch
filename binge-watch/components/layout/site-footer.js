export function SiteFooter() {
  return (
    <footer className="relative z-10 px-4 pb-8 pt-4 sm:px-6 lg:px-8">
      <div className="mx-auto flex max-w-7xl flex-col justify-between gap-3 rounded-[1.75rem] border border-white/8 bg-black/20 px-6 py-5 text-sm text-slate-400 sm:flex-row">
        <p>BingeWatch is built for curated external video embeds, not file hosting.</p>
        <p>Next.js + MongoDB + JWT + custom player controls.</p>
      </div>
    </footer>
  );
}
